# EQ Activity Date Finder

Ward activity planning calendar (Aug 2026 – Dec 2027), deployed via GitHub Pages.

- **Live page:** https://wyatt-oi.github.io/eq-activity-date-finder/
- **Request a change:** use the "✎ Suggest a change" button on the page (or click any day) —
  it files an issue here labeled `change-request`.
- `index.html` is a deployed copy — the source of truth lives in a separate private repo.
  Don't edit it here; changes land via that repo's update flow.
